# Shared evidence — Survival (all variants)

STATUS: empty, under construction. Do not populate by hand.

This file gets populated ONLY once at least 2 variants under
`variants/` are already "mature" (see the rule in `../SKILL.md`) and their
promoted patterns are compared to check whether something converges
across different variants too (e.g. a pacing pattern that holds for both
loop-100 and space-war-2team, despite being structurally different loops).

If, after comparison, nothing converges, this file stays empty — that's a
correct outcome: it means Survival, in your case, needs to be handled per
variant and has no common core generalizable at the genre level.

Format once populated:

## <shared pattern>
- Observed in: <variant A> (maps: ...), <variant B> (maps: ...)
- Plausible cause: ...
- Actionable recommendation: ...
