# Item pool / round progression patterns

One-line pattern entries, replace the placeholder as real ones get validated.

- (example — replace) item pool rotation that avoids repeats until the pool is exhausted, then
  reshuffles — instead of pure random-with-replacement, which repeats too often to feel fair.
- (example — replace) round/phase progression driven by a single authoritative timer + state
  machine (see `state-machine.md`), not by each device independently guessing what phase it is.

For a specific, validated, full implementation of one of these (not just the shape) — check the
Obsidian second brain via `second-brain-query` first; a real device may already exist to adapt.
