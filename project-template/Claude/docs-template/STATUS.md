# Project status

<!-- planner-docs owns this whole file. The "Current state" block below is REPLACED on every
     update (it reflects right now, not history) — the dated log under it is what's append-only,
     newest entry on top, never deleted. Opening this file should make "where are we" obvious in
     ten seconds without reading the log. -->

## Current state
_(updated: YYYY-MM-DD)_

**In progress:** (empty — no task currently "In progress" in ROADMAP.md)

**Planned next (this release):** (empty — see ROADMAP.md's Tasks table, Priority = current MVP/release target)

**Done so far (this release):** (empty)

**Recommended next step:** (empty)

---

## Log

<!-- New entry AT THE TOP on every update. Don't delete history. -->

### YYYY-MM-DD (example, replace)

**Done:** (empty)

**Known issues:** (empty)

**Next step:** (empty)
